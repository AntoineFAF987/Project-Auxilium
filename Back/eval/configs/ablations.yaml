{
  "schema_version": 1,
  "benchmark_name": "auxilium-current-retrieval-ablations-pilot",
  "base_config": "eval/configs/current.yaml",
  "output_dir": "eval/reports",
  "warmup": true,
  "annotation_policy": "Minimal sufficient evidence; prefer the original message over quoted duplicates when both carry the same fact.",
  "variants": [
    "dense_only",
    "bm25_only",
    "hybrid_current",
    "hybrid_no_mmr",
    "hybrid_no_reranker"
  ]
}
